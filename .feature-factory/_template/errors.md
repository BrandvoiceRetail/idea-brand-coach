# Error Handling — <feature-slug>

> Bucket: Error handling & retry · Orchestrator: `errors-orchestrator`

The error matrix: every failure mode enumerated, each retry policy decided, each PII field protected.
For this app: Supabase/edge-function failures surface as `sonner` toasts with non-technical messages.

_TODO: fill via the errors-orchestrator skill._
