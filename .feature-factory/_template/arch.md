# Architecture — <feature-slug>

> Bucket: Architecture · Orchestrator: `arch-orchestrator`

Where this feature fits, what it blocks / is blocked by, its critical-path position, and the
dependency graph. Cite real file paths (with line numbers) when referencing existing code.

_TODO: fill via the arch-orchestrator skill._
