import { AuthenticityModule } from "@/components/AuthenticityModule";

export default function AuthenticityLearning() {
  return (
    <div className="container mx-auto py-8">
      <AuthenticityModule />
    </div>
  );
}